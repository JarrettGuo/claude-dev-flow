# Output Style

本文档定义 claude-dev-flow 框架下所有 command 和 agent 的**终端输出风格**。

目标：**减少噪音，保留信号**。让用户在一屏之内看清流程状态，而不是淹没在 bash 调用细节里。

---

## 一、核心原则

### 1.1 显示什么

✅ **必须显示**：
- 检查**结论**（如 `✓ 预检通过`、`✗ 工作区不干净`）
- 需要用户决策的**信息列表**（如 `git status` 的文件清单）
- 需要用户回答的**问题**（如 `继续吗？[y/n]`）
- 流程关键节点（phase 切换、agent 启动/退出、重要决策）
- FLOW.log 的终端回显（见 `.claude/skills/flow-log/SKILL.md`，这是框架刻意设计的防走岔机制，**不算噪音**）

❌ **不得显示**：
- bash 检查命令的**原始代码**（`if [ ! -d ".claude" ]; then ...` 这种）
- 多个检查命令的**逐个展开**（合并成一条结论）
- 同一信息的**重复展示**（如原始 `git status` 输出 + 重新格式化为中文列表，只保留一次）
- AI 的**自述式叙述**（"我要检查 X，然后执行 Y，结果是 Z" 这种三段式废话）

### 1.2 合并原则

多个相邻的检查命令，如果结果都是"通过"，合并成**一条**确认消息。

例：
- ❌ 坏：分别展示 `.claude/ 存在 ✓`、`git 分支是 main ✓`、`工作区干净 ✓` 三条
- ✅ 好：`✓ 预检通过：.claude 存在，main 分支，工作区干净`

只有在**失败**或**需要展示详情**时才拆开展示。

### 1.3 不要自述

AI 在执行命令时，**不要**用自然语言叙述自己在做什么，让工具调用和结果说话。

例：
- ❌ 坏：
  > "我将先检查目录是否存在。"
  > [执行 bash]
  > "结果显示目录存在。接下来我要检查分支..."

- ✅ 好：直接执行，只在**结论**或**需要决策时**说话。

---

## 二、按场景分类

### 2.1 Command 的输出风格

Command 是用户触发的入口，输出应当**简洁、有节奏、有明确的等待点**。

**预检阶段**：
- 所有预检命令合并执行，结果合成一句
- 任何失败，单独拎出来展示 + 给出建议

**执行阶段**：
- 跟随 `flow-log` 的日志输出（默认只写文件，不输出到 stderr；Claude Code 用户直接在日志文件里看进度）
- 纯 CLI 用户可设置 `FLOW_LOG_STDERR=1` 开启终端输出
- 不在 flow-log 之外加额外的"我正在做 X" 叙述

**确认阶段**：
- 问题一次只问一个
- 展示必要的决策上下文（如改动文件列表、建议的 commit message）
- 不在问题之外添加解释性废话

### 2.2 Agent 的输出风格

Agent 是被 command 调用的角色，其输出会被 command 整合。Agent 应当：

- **不重复** command 层已经展示过的信息
- 聚焦在**本 agent 的专属产出**（分析结论、设计文档、代码、review 意见等）
- 中间步骤通过 flow-log 记录，不直接打印到终端（除了 stderr 那一份）
- 结束时给出**一句话总结** + **产物路径**，不要逐条复述产物内容

### 2.3 元命令的输出风格（`/upgrade`、`/flow-debug` 等）

元命令通常在做框架维护，用户期望**运维脚本式**的简洁输出：

- 每个阶段 1-3 行总结
- 状态用符号而非整句（`✓` / `✗` / `⚠` / `⏭`）
- 异常才展开详情

---

## 三、反例（不要这么做）

### 反例 1：把 bash 命令本身当成叙述内容

❌ 坏：

```
执行预检：
Bash(if [ ! -d ".claude" ]; then echo "..." ...) ✓ .claude/ 目录存在
Bash(if [ -n "$(git status --porcelain ...)" ]; then echo "..." ...) ⚠️ 工作区不干净
```

✅ 好：

```
⚠️ 预检失败：工作区有未提交改动
M package.json ?? docker-compose.dev.yml ?? client/Dockerfile.dev
建议先 git stash 或 commit，继续升级吗？[y/n]
```

### 反例 2：自述 + 重复

❌ 坏：

```
我将检查 git 状态。
[Bash: git status --porcelain]
 D [CLAUDE_GUIDE_review.md](CLAUDE_GUIDE_review.md)
 M package.json
...
好的，结果显示有以下未提交改动：
- D [CLAUDE_GUIDE_review.md](CLAUDE_GUIDE_review.md) (已删除)
- M package.json (已修改)
...
```

✅ 好：

```
⚠️ 未提交改动：
 D [CLAUDE_GUIDE_review.md](CLAUDE_GUIDE_review.md)
 M package.json ?? client/Dockerfile.dev ?? docker-compose.dev.yml ?? server/Dockerfile.dev
继续升级吗？[y/n]
```

### 反例 3：碎片化确认

❌ 坏：

```
第一步完成 ✓
继续第二步吗？[y/n]
[用户回 y]
第二步完成 ✓
继续第三步吗？[y/n]
...
```

✅ 好（预检阶段合批）：

```
✓ 预检通过
✓ 分支已建：feat/output-style
✓ 备份完成：/tmp/claude-dev-flow-backup-20260420-143022
开始主改动？[y/n]
```

---

## 四、例外情况

以下场景**可以**适当增加输出：

1. **出错时**：异常堆栈、错误详情、修复建议 —— 详细是有用的
2. **用户显式要求 verbose**：如 `--verbose` 参数或对话中说"详细一点"
3. **复杂决策点**：需要用户在多个选项中选择时，展示每个选项的利弊
4. **FLOW.log 终端回显**：这是 3.4 节决策的一部分，不受本文档限制

---

## 五、实现建议（给框架开发者）

- Command 的 bash 预检，**合并成一个脚本块**执行，不要拆成多次 `Bash()` 调用
- 使用 `set -e` 让预检在失败时立即停止，然后统一报错
- 展示列表时用 git 原生格式或对齐的列，不要自己发明格式
- 涉及文件清单、diff 等结构化信息，**直接展示原始输出**，不要再"翻译"一遍
